# Streamlit Community Cloud에서는 .env 대신 "Secrets"를 사용합니다.
# 아래 값은 TiDB Cloud(Serverless/Starter) 클러스터의 Connect 화면에서 확인해 입력하세요.

[connections.mysql]
host = "YOUR_TIDB_HOST"         # 예: gateway01.ap-northeast-1.prod.aws.tidbcloud.com
port = 4000                    # TiDB Serverless 기본 포트
database = "perfdb"
username = "YOUR_TIDB_USER"
password = "YOUR_TIDB_PASSWORD"

# (선택) TLS 에러가 나는 경우 CA 파일 경로를 지정할 수 있습니다.
# ssl_ca = "/path/to/ca.pem"
